import React from "react";
import { View, Text } from "react-native";
import * as Interface from "./interface";

const PrizeAnnouncement = ({ children }: Interface.IProps) => {
	return <View>{children}</View>;
};

export default PrizeAnnouncement;
