import { StyleSheet } from "react-native";

const style = StyleSheet.create({
	outer: {
		position: "absolute",
		marginHorizontal: 10,
		marginVertical: 10,
		top: 0,
		right: 0,
		left: 0,
	},
});

export default style;
