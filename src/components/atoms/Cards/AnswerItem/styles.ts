import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	outer: {
		width: "100%",
		padding: 10,
		marginTop: 10,
		marginBottom: 20,
		borderRadius: 5,
		elevation: 3,
		position: "relative",
	},
});

export default styles;
