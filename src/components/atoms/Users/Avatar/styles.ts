import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	outer: {
		height: 70,
		width: 70,
		borderRadius: 35,
		borderWidth: 3,
	},
	badgeOuter: {
		position: "absolute",
		bottom: -5,
		right: -5,
		borderRadius: 30,
		padding: 10,
		borderWidth: 2,
	},
});

export default styles;
